<h1><%= @professor.name %></h1>
<h2>Professor of <%=@professor.field%></h2>

<h3>Courses Taught:</h3>
<ul>
    <% @professor.courses.each do |course| %>
        <li> <%= link_to course.name, course %> | <%= course.time %></li>
    <% end %>
</ul>

<%= render partial: "shared/edit_delete_buttons", locals: {object: @professor} %>